package com.telebroad.teleconsole.chat.viewModels;

import android.content.Context;

import androidx.annotation.NonNull;
import androidx.lifecycle.MutableLiveData;
import androidx.lifecycle.ViewModel;
import androidx.lifecycle.ViewModelProvider;

//public class ViewModelFactory implements ViewModelProvider.Factory {
////    String topic;
////    Context context;
////
////
////    public ViewModelFactory(String topic,Context context) {
////        this.topic = topic;
////        this.context = context;
////    }
////
////    @NonNull
////    @Override
////    public <T extends ViewModel> T create(@NonNull Class<T> modelClass) {
////        if (modelClass.isAssignableFrom(SecondChatViewModel.class))
////        return (T) new SecondChatViewModel(topic,context);
////        else  throw new IllegalArgumentException("Unknown ViewModel class");
////    }
//}
